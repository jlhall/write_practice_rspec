require_relative '../practice_rspec'

describe "Rspec can use" do
  # what can (and commonly does) go up here?
  # before individual test cases?
  # but still inside the test suite?

  it "the #E matcher to check if a value is exactly what is expected"
  #square #E

  it "the #B matcher to check if an object is the same as another"
  #living_object #B

  it "the #I matcher to check if a given value is within the containing object"
  #in_object #I

  it "the #M matcher to check if the given object mirrors another"
  #mirror_object #M #RegEx

end
